package com.example.suneet.onroadwheels;

import android.content.Context;
import android.support.v7.widget.RecyclerView;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

/**
 * Created by suneet on 29/7/17.
 */

/*
public class SearchItemRecyclerAdapter extends RecyclerView.Adapter<SearchItemRecyclerAdapter.ViewHolder> {
    ArrayList<EmergencyContact> contactArrayList;
    Context c;

    public SearchItemRecyclerAdapter(ArrayList<EmergencyContact> contactArrayList, Context c) {
        this.contactArrayList = contactArrayList;
        this.c = c;
    }

    @Override
    public SearchItemRecyclerAdapter.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        return new ViewHolder(LayoutInflater.from(c).inflate(R.layout.search_screen_item,parent,false));
    }

    @Override
    public void onBindViewHolder(SearchItemRecyclerAdapter.ViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    public class ViewHolder extends RecyclerView.ViewHolder {
        public ViewHolder(View itemView) {
            super(itemView);
        }
    }
}
*/
